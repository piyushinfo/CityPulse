import AppShell from "../components/AppShell";
import { useLiveState, STATUS_COLOR } from "../lib/api";
import Icon from "../components/Icon";

export default function Forecast(){const {state:s}=useLiveState(); if(!s)return <AppShell active="/forecast" title="Forecast"><main className="page page-center">Loading forecast…</main></AppShell>; const sorted=[...s.forecast].sort((a,b)=>a.next_hour-b.next_hour); return <AppShell active="/forecast" title="Next-Hour Outlook"><main className="page"><div className="page-hero"><div><div className="eyebrow">EXPLAINABLE OUTLOOK</div><h1>Next-hour forecast</h1><p>Transparent estimate using recent pulse trend, next-hour weather and comparable historical conditions.</p></div><div className="weather-card"><Icon name="cloud" size={18}/><div><b>{s.forecast_meta?.rain_next_hour} mm/h</b><span>rain · {Math.round(s.forecast_meta?.temp_next_hour)}°C</span></div></div></div><div className="forecast-banner panel"><div className="banner-icon"><Icon name="activity" size={22}/></div><div><div className="eyebrow">CITY VIEW</div><b>{s.city.score} → {Math.round(sorted.reduce((sum,f)=>sum+f.next_hour,0)/Math.max(1,sorted.length))}</b><p>{sorted.filter(f=>f.direction==='worse').length} zones may worsen · {sorted.filter(f=>f.direction==='better').length} may improve.</p></div></div><div className="forecast-grid">{sorted.map(f=>{const z=s.zones.find(x=>x.id===f.zone_id); const color=f.direction==='worse'?STATUS_COLOR.alert:f.direction==='better'?STATUS_COLOR.calm:STATUS_COLOR.watch; return <article className="forecast-card" key={f.zone_id}><div className="forecast-head"><div><small>{z?.name||f.zone_id}</small><b>{f.now} → {f.next_hour}</b></div><span style={{color}}>{f.direction==='worse'?'↓':f.direction==='better'?'↑':'→'}</span></div><div className="forecast-meter"><span style={{width:`${f.next_hour}%`,background:color}}/></div><p>{f.reason_en}</p><div className="forecast-hi">{f.reason_hi}</div></article>})}</div><section className="panel"><div className="panel-head"><div><div className="eyebrow">METHOD</div><h2>Why this is not a black-box prediction</h2></div></div><div className="method-grid"><div><span>01</span><b>Recent trend</b><p>Current pulse is compared with its value around 15 minutes earlier.</p></div><div><span>02</span><b>Weather</b><p>Next-hour rain and temperature can shift the projected score.</p></div><div><span>03</span><b>History</b><p>Three-day condition profiles show how each zone behaved under comparable weather.</p></div></div></section></main></AppShell>}
