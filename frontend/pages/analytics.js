import { useEffect, useMemo, useState } from "react";
import AppShell from "../components/AppShell";
import TrendChart from "../components/TrendChart";
import { getJSON, useLiveState, STATUS_COLOR } from "../lib/api";

export default function Analytics(){const {state:s}=useLiveState(); const [a,setA]=useState(null); const [hours,setHours]=useState(6); useEffect(()=>{getJSON(`/api/analytics?hours=${hours}&interval_min=15`).then(setA).catch(()=>{})},[hours]); const city=a?.points||[]; const rank=useMemo(()=>[...(s?.zones||[])].sort((x,y)=>x.score-y.score),[s]); return <AppShell active="/analytics" title="Analytics"><main className="page"><div className="page-hero"><div><div className="eyebrow">DATA SCIENCE / HEALTH TRENDS</div><h1>Analytics</h1><p>Explainable trends, anomaly activity and zone comparisons from the same event history used by the engine.</p></div><select className="select compact" value={hours} onChange={e=>setHours(Number(e.target.value))}><option value="6">Last 6h</option><option value="12">Last 12h</option><option value="24">Last 24h</option><option value="72">Last 3d</option></select></div><div className="grid-two"><section className="panel chart-large"><div className="panel-head"><div><div className="eyebrow">CITY PULSE</div><h2>Stress trajectory</h2></div><div className="stat-inline"><b>{s?.city.score||'—'}</b><span>/100 current</span></div></div><TrendChart points={city.map(p=>({score:p.city_score}))} valueKey="score" height={240}/><div className="chart-axis-note"><span>{city[0]?.ts?.slice(11,16)||''}</span><span>{city[Math.floor(city.length/2)]?.ts?.slice(11,16)||''}</span><span>{city[city.length-1]?.ts?.slice(11,16)||''}</span></div></section><section className="panel"><div className="panel-head"><div><div className="eyebrow">SIGNAL MIX</div><h2>Current anomaly categories</h2></div></div>{Object.entries(a?.category_breakdown||{}).length?Object.entries(a.category_breakdown).map(([k,v])=><div key={k} className="bar-stat"><div><b>{k}</b><span>{v} active</span></div><div className="long-bar"><span style={{width:`${Math.min(100,v*18)}%`}}/></div></div>):<div className="empty-state">No active categories.</div>}<div className="divider"/><div className="analytics-note">The pulse is a composite stress indicator. It is not an official government safety rating.</div></section></div><section className="panel"><div className="panel-head"><div><div className="eyebrow">ZONE COMPARISON</div><h2>Who is moving?</h2></div></div><div className="zone-compare">{rank.map(z=><div className="compare-row" key={z.id}><div className="compare-name">{z.name}</div><div className="compare-bar"><span style={{width:`${z.score}%`,background:STATUS_COLOR[z.status]}}/></div><b style={{color:STATUS_COLOR[z.status]}}>{z.score}</b></div>)}</div></section><section className="panel"><div className="panel-head"><div><div className="eyebrow">TECHNICAL NOTE</div><h2>How to explain this page</h2></div></div><div className="explain-grid"><div><b>Window</b><span>3h rolling window for anomaly detection.</span></div><div><b>Baseline</b><span>Three-day generated history for normal activity.</span></div><div><b>Pulse</b><span>Category weight × anomaly severity, combined multiplicatively.</span></div><div><b>Forecast</b><span>Recent trend + next-hour weather + historical condition profile.</span></div></div></section></main></AppShell>}
